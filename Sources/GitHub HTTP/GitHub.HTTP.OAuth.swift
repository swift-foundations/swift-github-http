extension GitHub.HTTP {
    // swift-linter:disable:next compound type name
    // REASON: rule-corpus false positive — nested Nest.Name declaration flagged as compound; queued for #16 Option C adjudication.
    public enum OAuth: Sendable {}
}
